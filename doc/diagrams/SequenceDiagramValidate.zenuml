GVR.request(pan) {
  isValidRequest = Validator.isValidRequest(pan)
  if(isValidRequest) {
    transactionCredentials = HSM.generateCredentials()
    Database.persist(transactionCredentials)
  }
  else {
    return "error 400"
  }
  return transactionCredentials
}